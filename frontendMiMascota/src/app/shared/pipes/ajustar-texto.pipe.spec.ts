import { AjustarTextoPipe } from './ajustar-texto.pipe';

describe('AjustarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new AjustarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
